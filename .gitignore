# secrets — never commit these
.env
.env.local
keys.env
*.key

# local-only privacy sweep deny list (machine names, your subnet, ...)
.privacy-deny.local

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# editors / os
.idea/
.vscode/
.DS_Store

# local backups of .env (the sweep scans tracked files; keep them out of git)
.env.bak*
